sources:
  - name: jaffle_shop
    database: raw
    #schema: jaffle_shop (optional but best to add both -same as name)
    #source table names
    tables:
      - name: customers
      - name: orders
        config:
          freshness:
            warn_after:
              count: 24
              period: minute
            error_after:
              count: 48
              period: hour
              
          loaded_at_field: _etl_loaded_at